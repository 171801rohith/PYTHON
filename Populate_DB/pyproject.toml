[project]
name = "populate-db"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.12.4",
    "psycopg2>=2.9.10",
    "sqlalchemy>=2.0.43",
]
